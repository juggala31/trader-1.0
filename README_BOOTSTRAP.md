# Quickstart (MVP)
1) Create a venv (optional):
   py -3 -m venv .venv
   .venv\Scripts\activate

2) Install:
   pip install -e .

3) Run all:
   python -m exosati_trader.app.start_all

Or use the batch:
   tools\start_enhanced_dashboard.bat